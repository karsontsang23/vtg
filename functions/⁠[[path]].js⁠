// 💡 徹底拿走了第一行的 import fflate，不依賴任何外部檔案
export async function onRequest(context) {
  return new Response("【地獄級除錯成功】如果你見到呢行字，代表核心管道完全正常！真兇就係 fflate 套件喺 Pages 運作唔到。", {
    headers: { "Content-Type": "text/plain; charset=utf-8" }
  });
}
