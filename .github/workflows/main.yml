# ... 前面步驟保持不變 ...
      - name: Convert to GIF
        run: |
          ffmpeg -i input.mp4 -vf "fps=15,scale=480:-1:flags=lanczos,split[s0][s1];[s0]palettegen[p];[s1][p]paletteuse" output.gif

      # 保持使用 GitHub 官方 Artifact 儲存成品[span_1](start_span)[span_1](end_span)
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.job_id }}
          path: output.gif

      # 💡 受影響代碼修改：直接將免驗證的 nightly.link 下載網址傳回 Worker[span_2](start_span)[span_2](end_span)
      - name: Notify Cloudflare Worker (Webhook)
        run: |
          curl -X POST https://YOUR_WORKER.workers.dev/callback \
            -H "Content-Type: application/json" \
            -d '{
              "job_id": "${{ inputs.job_id }}",
              "gif_url": "https://nightly.link/${{ github.repository }}/actions/artifacts/${{ github.run_id }}.zip"
            }'
