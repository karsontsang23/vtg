export default {
  async fetch(req, env) {
    // 💡 移除所有外部 import 與複雜邏輯，確保 100% 能正常啟動
    return new Response(JSON.stringify({ 
      status: "success", 
      message: "系統調試：如果你見到呢行字，代表 Pages 管道通咗！問題出喺 fflate 套件打包。" 
    }), {
      headers: { "Content-Type": "application/json; charset=utf-8" }
    });
  }
};
